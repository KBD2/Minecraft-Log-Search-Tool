Version 1.0.0

This is meant to be used as a command line application, but it can be run in the IDLE.

Arguments:

-search		Specifies the string to search for
-dir		Optional, specifies a custom directory to search in, if not given will try to use the default directory
-output		Optional, specifies an output file